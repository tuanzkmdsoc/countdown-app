{
  "name": "countdown-app",
  "version": "1.0.0",
  "description": "Trang web đếm ngược với đăng nhập chỉ bằng mật khẩu (Admin sửa / User chỉ xem). Mật khẩu được lưu dưới dạng đã băm (hash) ở backend, không ai xem được ở frontend.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "hash-password": "node scripts/hash-password.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.6",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2"
  }
}
